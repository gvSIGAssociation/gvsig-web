// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.gva.dgti.gvgeoportal.domain;

import es.gva.dgti.gvgeoportal.domain.AgrupadorCapaServicioWebDataOnDemand;
import es.gva.dgti.gvgeoportal.domain.AgrupadorCapaServicioWebIntegrationTest;
import es.gva.dgti.gvgeoportal.service.domain.AgrupadorCapaServicioWebService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AgrupadorCapaServicioWebIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AgrupadorCapaServicioWebIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AgrupadorCapaServicioWebIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AgrupadorCapaServicioWebIntegrationTest: @Transactional;
    
    @Autowired
    AgrupadorCapaServicioWebDataOnDemand AgrupadorCapaServicioWebIntegrationTest.dod;
    
    @Autowired
    AgrupadorCapaServicioWebService AgrupadorCapaServicioWebIntegrationTest.agrupadorCapaServicioWebService;
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testCountAllAgrupadorCapaServicioWebs() {
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", dod.getRandomAgrupadorCapaServicioWeb());
        long count = agrupadorCapaServicioWebService.countAllAgrupadorCapaServicioWebs();
        Assert.assertTrue("Counter for 'AgrupadorCapaServicioWeb' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testFindAgrupadorCapaServicioWeb() {
        AgrupadorCapaServicioWeb obj = dod.getRandomAgrupadorCapaServicioWeb();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to provide an identifier", id);
        obj = agrupadorCapaServicioWebService.findAgrupadorCapaServicioWeb(id);
        Assert.assertNotNull("Find method for 'AgrupadorCapaServicioWeb' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'AgrupadorCapaServicioWeb' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testFindAllAgrupadorCapaServicioWebs() {
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", dod.getRandomAgrupadorCapaServicioWeb());
        long count = agrupadorCapaServicioWebService.countAllAgrupadorCapaServicioWebs();
        Assert.assertTrue("Too expensive to perform a find all test for 'AgrupadorCapaServicioWeb', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<AgrupadorCapaServicioWeb> result = agrupadorCapaServicioWebService.findAllAgrupadorCapaServicioWebs();
        Assert.assertNotNull("Find all method for 'AgrupadorCapaServicioWeb' illegally returned null", result);
        Assert.assertTrue("Find all method for 'AgrupadorCapaServicioWeb' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testFindAgrupadorCapaServicioWebEntries() {
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", dod.getRandomAgrupadorCapaServicioWeb());
        long count = agrupadorCapaServicioWebService.countAllAgrupadorCapaServicioWebs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<AgrupadorCapaServicioWeb> result = agrupadorCapaServicioWebService.findAgrupadorCapaServicioWebEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'AgrupadorCapaServicioWeb' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'AgrupadorCapaServicioWeb' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testFlush() {
        AgrupadorCapaServicioWeb obj = dod.getRandomAgrupadorCapaServicioWeb();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to provide an identifier", id);
        obj = agrupadorCapaServicioWebService.findAgrupadorCapaServicioWeb(id);
        Assert.assertNotNull("Find method for 'AgrupadorCapaServicioWeb' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAgrupadorCapaServicioWeb(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'AgrupadorCapaServicioWeb' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testUpdateAgrupadorCapaServicioWebUpdate() {
        AgrupadorCapaServicioWeb obj = dod.getRandomAgrupadorCapaServicioWeb();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to provide an identifier", id);
        obj = agrupadorCapaServicioWebService.findAgrupadorCapaServicioWeb(id);
        boolean modified =  dod.modifyAgrupadorCapaServicioWeb(obj);
        Integer currentVersion = obj.getVersion();
        AgrupadorCapaServicioWeb merged = agrupadorCapaServicioWebService.updateAgrupadorCapaServicioWeb(obj);
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'AgrupadorCapaServicioWeb' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testSaveAgrupadorCapaServicioWeb() {
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", dod.getRandomAgrupadorCapaServicioWeb());
        AgrupadorCapaServicioWeb obj = dod.getNewTransientAgrupadorCapaServicioWeb(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'AgrupadorCapaServicioWeb' identifier to be null", obj.getId());
        try {
            agrupadorCapaServicioWebService.saveAgrupadorCapaServicioWeb(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'AgrupadorCapaServicioWeb' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AgrupadorCapaServicioWebIntegrationTest.testDeleteAgrupadorCapaServicioWeb() {
        AgrupadorCapaServicioWeb obj = dod.getRandomAgrupadorCapaServicioWeb();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'AgrupadorCapaServicioWeb' failed to provide an identifier", id);
        obj = agrupadorCapaServicioWebService.findAgrupadorCapaServicioWeb(id);
        agrupadorCapaServicioWebService.deleteAgrupadorCapaServicioWeb(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'AgrupadorCapaServicioWeb' with identifier '" + id + "'", agrupadorCapaServicioWebService.findAgrupadorCapaServicioWeb(id));
    }
    
}
