// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.gva.dgti.gvgeoportal.domain;

import es.gva.dgti.gvgeoportal.domain.GeoportalServicioWebDataOnDemand;
import es.gva.dgti.gvgeoportal.domain.GeoportalServicioWebIntegrationTest;
import es.gva.dgti.gvgeoportal.service.domain.GeoportalServicioWebService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect GeoportalServicioWebIntegrationTest_Roo_IntegrationTest {
    
    declare @type: GeoportalServicioWebIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: GeoportalServicioWebIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: GeoportalServicioWebIntegrationTest: @Transactional;
    
    @Autowired
    GeoportalServicioWebDataOnDemand GeoportalServicioWebIntegrationTest.dod;
    
    @Autowired
    GeoportalServicioWebService GeoportalServicioWebIntegrationTest.geoportalServicioWebService;
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testCountAllGeoportalServicioWebs() {
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", dod.getRandomGeoportalServicioWeb());
        long count = geoportalServicioWebService.countAllGeoportalServicioWebs();
        Assert.assertTrue("Counter for 'GeoportalServicioWeb' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testFindGeoportalServicioWeb() {
        GeoportalServicioWeb obj = dod.getRandomGeoportalServicioWeb();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to provide an identifier", id);
        obj = geoportalServicioWebService.findGeoportalServicioWeb(id);
        Assert.assertNotNull("Find method for 'GeoportalServicioWeb' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'GeoportalServicioWeb' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testFindAllGeoportalServicioWebs() {
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", dod.getRandomGeoportalServicioWeb());
        long count = geoportalServicioWebService.countAllGeoportalServicioWebs();
        Assert.assertTrue("Too expensive to perform a find all test for 'GeoportalServicioWeb', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<GeoportalServicioWeb> result = geoportalServicioWebService.findAllGeoportalServicioWebs();
        Assert.assertNotNull("Find all method for 'GeoportalServicioWeb' illegally returned null", result);
        Assert.assertTrue("Find all method for 'GeoportalServicioWeb' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testFindGeoportalServicioWebEntries() {
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", dod.getRandomGeoportalServicioWeb());
        long count = geoportalServicioWebService.countAllGeoportalServicioWebs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<GeoportalServicioWeb> result = geoportalServicioWebService.findGeoportalServicioWebEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'GeoportalServicioWeb' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'GeoportalServicioWeb' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testFlush() {
        GeoportalServicioWeb obj = dod.getRandomGeoportalServicioWeb();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to provide an identifier", id);
        obj = geoportalServicioWebService.findGeoportalServicioWeb(id);
        Assert.assertNotNull("Find method for 'GeoportalServicioWeb' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyGeoportalServicioWeb(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'GeoportalServicioWeb' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testUpdateGeoportalServicioWebUpdate() {
        GeoportalServicioWeb obj = dod.getRandomGeoportalServicioWeb();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to provide an identifier", id);
        obj = geoportalServicioWebService.findGeoportalServicioWeb(id);
        boolean modified =  dod.modifyGeoportalServicioWeb(obj);
        Integer currentVersion = obj.getVersion();
        GeoportalServicioWeb merged = geoportalServicioWebService.updateGeoportalServicioWeb(obj);
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'GeoportalServicioWeb' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testSaveGeoportalServicioWeb() {
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", dod.getRandomGeoportalServicioWeb());
        GeoportalServicioWeb obj = dod.getNewTransientGeoportalServicioWeb(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'GeoportalServicioWeb' identifier to be null", obj.getId());
        try {
            geoportalServicioWebService.saveGeoportalServicioWeb(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'GeoportalServicioWeb' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void GeoportalServicioWebIntegrationTest.testDeleteGeoportalServicioWeb() {
        GeoportalServicioWeb obj = dod.getRandomGeoportalServicioWeb();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'GeoportalServicioWeb' failed to provide an identifier", id);
        obj = geoportalServicioWebService.findGeoportalServicioWeb(id);
        geoportalServicioWebService.deleteGeoportalServicioWeb(obj);
        obj.flush();
        Assert.assertNull("Failed to remove 'GeoportalServicioWeb' with identifier '" + id + "'", geoportalServicioWebService.findGeoportalServicioWeb(id));
    }
    
}
